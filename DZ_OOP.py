class TheMetod:
        def metod1(self, string):
            self.glas = 'aouyei'
            self.overall = len(string)
            self.vow = ''
            self.cons = ''
            for i in metod1:
                if i in self.glas:
                    self.vow += i
                else:
                    self.cons += i
                if len(self.vow) * len(self.cons) <= self.overall:
                    return self.vow
            return self.cons


        def metod2 (self):
            return len(str())

